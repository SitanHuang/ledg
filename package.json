{
  "name": "ledg",
  "version": "2.0.0-dev",
  "type": "module",
  "scripts": {
    "clean": "rm -rf out/*",
    "build": "tsc",
    "test": "vitest run --config lib/vitest.config.ts --coverage",
    "buildtest": "tsc && vitest run --config lib/vitest.config.ts --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "@vitest/coverage-v8": "^3.0.9",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
